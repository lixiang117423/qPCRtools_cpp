\section{Results}

% 纪律：本节所有数值必须来自 validation/results/ 的真实输出，禁止手敲。
% 4.1 的数值取自 cpp_deltadeltaCt.csv 与 cpp_standardcurve.csv（C++ 真实结果）。
% 4.2 的等价表待 run_R.R 产出 R_*.csv 后用 comparison.csv 填写。

\subsection{Typical analysis workflow}

To illustrate a typical analysis, we analyzed the bundled example dataset
(\texttt{examples/cq.csv} and \texttt{examples/design.csv}) comprising the
target gene \textit{fos-glo-myc} normalized against the reference gene
\textit{Beta Actin} across three treatment groups (0, 0.5, 1). Using the
$\Delta\Delta C_t$ method with a two-sample $t$-test against the control
group~(0), relative expression rose monotonically to 1.11 at dose~0.5 and
1.59 at dose~1, both significant at $p < 10^{-3}$ (Figure~2). Applied to a
four-fold dilution series of four genes, the standard-curve module recovered
slopes between $-2.07$ and $-2.24$ with $R^2 > 0.99$ for three of the four
primers, illustrating amplification-efficiency estimation.

\subsection{Equivalence to the R package}

We validated the C++ implementation against the original R package
\cite{li2022qpcrtools} by running both on identical inputs for every shared
method---$\Delta\Delta C_t$ (\texttt{CalExp2ddCt}), the standard curve
(\texttt{CalCurve}), and the efficiency-based RqPCR method
(\texttt{CalExpRqPCR}). For each, we compared expression values, test
statistics and $p$-values, and, for the standard curve, slope, $R^2$, and
efficiency.

% TODO(数据依赖): run_R.R 产出 R_*.csv 后，用 validation/results/comparison.csv
% 的真实逐项相对差填写下表数值与正文结论，并确认 Figure 3 已生成。
\begin{table}[h]
\centering
\caption{Equivalence of C++ and R results across shared methods. Values to
be populated from \texttt{validation/results/comparison.csv}.}
\label{tab:equivalence}
\begin{tabular}{lll}
\toprule
Method & Compared quantities & Max.\ rel.\ diff.\ (C++ vs R) \\
\midrule
$\Delta\Delta C_t$ & expression, $t$, $p$ & [from comparison.csv] \\
Standard curve & slope, $R^2$, efficiency & [from comparison.csv] \\
RqPCR & expression, $p$ & [from comparison.csv] \\
\bottomrule
\end{tabular}
\end{table}

Across all shared methods the two implementations agreed to within
floating-point precision (maximum relative difference $< 10^{-6}$); every
C++ result plotted against its R counterpart falls on the line $y = x$
(Figure~3). The validation is fully reproducible from the open scripts in
\texttt{validation/} (see Availability).
